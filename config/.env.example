# Frontend (Vite) - Used for development and testing
VITE_RELEASES_URL=https://raw.githubusercontent.com/voboost/voboost-install/main/releases.json

# Backend (Rust) - Set at build time to override the default releases.json URL
# Usage: RELEASES_URL=https://example.com/releases.json cargo build
# Default: https://raw.githubusercontent.com/voboost/voboost-install/main/releases.json
# RELEASES_URL=
